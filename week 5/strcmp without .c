#include <stdio.h>
#include <string.h>
#include<stdio.h>
int main()
{
char st1,st2,i;
int i,flag;
printf ("Enter string 1 to read");
gets(st1);
Printf("Enter string 2 to read");
gets(st2);
while (st1[i]!=st2[i)]
{
flag = 1;
break;
}
i++;
if(flag=0)
{
printf ("equal")
}
 printf(" Equal"); {
else
{
printf("Not Equal");
}
return 0;
}
