#include <stdio.h>

int main() {
    int a, area;
    printf("Enter the value of a:\n");
    scanf("%d", &a);
    area = a * a;
    printf("The area of square = %d\n", area);
    return 0;
}
