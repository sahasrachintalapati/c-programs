#include <stdio.h>
int main()
{
    int b, h, area;
    printf("Enter the value of b and h \n");
    scanf("%d %d", &b, &h);
    area = b * h;
    printf("The area of parallelogram is %d", area);
    return 0;
}
