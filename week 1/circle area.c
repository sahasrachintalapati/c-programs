#include <stdio.h>
int main()
{
    float r, Area;
    printf("Enter the value of r \n");
    scanf("%f", &r);
    Area = (3.14) * r * r;
    printf("The area of circle is %f\n", Area);
    return 0;
}
